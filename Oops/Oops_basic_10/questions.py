# Question 1:
# Define a class `Person` with attributes `name` and `age`.
# Add a method `greet` that prints "Hello, my name is {name} and I am {age} years old."

# Question 2:
# Create a class `BankAccount` with attributes `account_number` and `balance`.
# Add methods `deposit(amount)` and `withdraw(amount)` to modify the balance.
# Ensure that the balance cannot go negative.

# Question 3:
# Implement a class `Rectangle` with attributes `length` and `breadth`.
# Add a method `area` to calculate and return the area of the rectangle.

# Question 4:
# Define a class `Student` with attributes `name`, `marks`, and `subject`.
# Add a method `is_passed` that checks if the marks are greater than or equal to 40.

# Question 5:
# Create a class `Vehicle` with attributes `brand` and `model`.
# Add a method `details` that prints the brand and model of the vehicle.
# Inherit this class into a class `Car` and add a new attribute `fuel_type`.

# Question 6:
# Define a class `Employee` with attributes `name` and `salary`.
# Add a method `give_raise` that increases the salary by a given percentage.
# Add another method `display` that prints the name and updated salary.

# Question 7:
# Implement a class `Calculator` with methods `add`, `subtract`, `multiply`, and `divide`.
# Each method should take two numbers as input and perform the respective operations.

# Question 8:
# Create a class `Animal` with attributes `name` and `species`.
# Add a method `speak` that prints "I am a {species} named {name}."
# Inherit this class into a class `Dog` and override the `speak` method to print "Woof! I am {name}."

# Question 9:
# Define a class `Library` with attributes `name` and `books` (a list of book names).
# Add methods `add_book` to add a book to the list and `list_books` to display all books.

# Question 10:
# Implement a class `Circle` with attribute `radius`.
# Add a method `circumference` to calculate and return the circumference of the circle.
# Add another method `area` to calculate and return the area of the circle.
